{
    "StaticFiles": "../../../../src", //Relative to running exe
    "Domain": "", //NTLM domain
    "UserName": "", //Leave empty to use current logged in user when in windows
    "Password": "" //Password as base64 just to avoid memorization. still unsecure
}
